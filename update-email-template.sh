#!/bin/bash

# Extract the generateConfirmationEmailHTML function from email-preview/page.tsx
# and update it in emailService.ts

echo "Updating email template in emailService.ts..."
echo "This will replace the old template with the dark theme template from email-preview page"
echo "Backup created at: src/services/emailService.ts.backup"

